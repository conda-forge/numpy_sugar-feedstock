{% set name = "numpy-sugar" %}
{% set version = "1.5.4" %}
{% set sha256 = "0d2a8d1d8c21f33d40c6a4fed1d11157e970f2d49581812c9613e52c1c7989ac" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/numpy_sugar-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  noarch: python
  number: 0
  script: {{ PYTHON }} -m pip install . --no-deps -vv

requirements:
  host:
    - pip
    - python >=3.7
    - setuptools
    - wheel
  run:
    - dask >=2.18.0
    - numpy >=1.14.1
    - pytest >=6.1.2
    - python >=3.7
    - scipy >=1.4.1

test:
  requires:
    - pytest >=6.1.2
  imports:
    - numpy_sugar
  commands:
    - python -c "import sys; import numpy_sugar; sys.exit(numpy_sugar.test())"

about:
  home: https://github.com/limix/numpy-sugar
  license: MIT
  license_family: MIT
  license_file: LICENSE.md
  summary: Missing NumPy functionalities
  doc_url: https://numpy-sugar.readthedocs.io
  dev_url: https://github.com/limix/numpy-sugar

extra:
  recipe-maintainers:
    - Horta
